cannot handle:
 - containers:
   - any pointer trickery
   - fields of struct being allocated separately
   - use of allocators hidden in objects (arguably poor form)
 - derived allocators
 - inline allocation during a function call
 - pointer aliasing at all
 - global variables
 - recursion
 - multiple threads
 - using deinit() as a non-free
 - use-after-free, but could be easily extended
 - real programs :(
